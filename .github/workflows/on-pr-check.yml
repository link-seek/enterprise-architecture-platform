name: PR CI

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  pr-ci:
    uses: link-seek/issue-resolver/.github/workflows/pr-ci.yml@main
    with:
      enable-backend-tests: true
      enable-frontend-tests: true
      enable-integration: true
      e2e-filter: '--grep @smoke'
      frontend-build-env: '["VITE_API_URL=https://api.xieyucheng.top/api"]'
    secrets: inherit
