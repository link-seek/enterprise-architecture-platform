name: Build and Push Images

on:
  push:
    branches: [main]

permissions:
  contents: read
  packages: write
  actions: write

jobs:
  ci-cd:
    uses: link-seek/issue-resolver/.github/workflows/ci-cd-acr.yml@main
    with:
      ghcr-image: ghcr.io/link-seek/eap-backend
      oss-bucket: eap-frontend-xyc
      frontend-build-env: '["VITE_API_URL=https://api.xieyucheng.top/api"]'
    secrets: inherit
