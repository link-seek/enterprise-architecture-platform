# Minimal runtime image with pre-built binary
FROM debian:bookworm-slim
RUN apt-get update && \
    apt-get install -y --no-install-recommends \
        ca-certificates \
        libsqlite3-0 \
    && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY server-bin /app/server
COPY config /app/config
RUN mkdir -p /app/data
EXPOSE 8080
CMD ["/app/server"]
